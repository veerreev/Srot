#!/bin/sh

# Triggers a macOS notification banner
osascript -e 'display notification "Remember: Do not push Assets unzipped." with title "Pull Successful"'

# Prints to the terminal as a fallback
echo "========================================="
echo "REMINDER: Remember to unzip assets"
echo "========================================="

exit 0